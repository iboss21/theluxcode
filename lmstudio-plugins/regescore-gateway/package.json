{
  "name": "regescore-gateway",
  "version": "1.0.0",
  "private": true,
  "description": "RegesCore gateway: use any OpenAI-compatible /v1/chat/completions or Anthropic-compatible /v1/messages endpoint as a model inside LM Studio.",
  "author": "David (davidio.dev)",
  "homepage": "https://davidio.dev",
  "license": "MIT",
  "dependencies": {
    "@lmstudio/sdk": "^1.5.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.6.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test --experimental-strip-types src/gateway.test.ts"
  }
}
